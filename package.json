{
  "name": "web-harmonium",
  "version": "1.0.0",
  "private": true,
  "description": "A production-ready virtual harmonium for the browser",
  "scripts": {
    "dev": "npm run dev --prefix client",
    "build": "npm run build --prefix client",
    "preview": "npm run preview --prefix client"
  }
}
